# Task: Phase 1 — Scaffold 初始化執行清單

**對應 plan：** `/plan/20260628-01-init.md`
**對應 spec：** `/spec/20260628-01-init.md`
**建立日期：** 2026-06-28
**狀態圖示：** `[ ]` 待執行 ｜ `[~]` 進行中 ｜ `[x]` 完成 ｜ `[!]` 阻塞

---

## T0 — 環境確認（執行前檢查）

- [ ] **T0-1** 確認 Node.js 版本 >= 20  
  `node -v`
- [ ] **T0-2** 確認 Java 版本 >= 21  
  `java -version`
- [ ] **T0-3** 確認 Maven 或 mvnw 可用  
  `mvn -v` 或 後端 scaffold 後用 `./mvnw -v`
- [ ] **T0-4** 確認 TravelProject/ 根目錄已有 git repo  
  `git -C /Users/eyesc/Project/TravelProject status`

---

## T1 — 前端 Scaffold（create-next-app）

- [ ] **T1-1** 執行 create-next-app 建立基礎專案
  ```bash
  cd /Users/eyesc/Project/TravelProject
  npx create-next-app@latest travel-frontend \
    --typescript \
    --tailwind \
    --app \
    --eslint \
    --no-src-dir \
    --import-alias "@/*"
  ```
- [ ] **T1-2** 安裝追加 dependencies
  ```bash
  cd travel-frontend
  npm install framer-motion date-fns clsx
  ```
- [ ] **T1-3** 確認 `package.json` 包含所有必要套件（next@15、framer-motion、date-fns、clsx）

---

## T2 — 前端目錄與 Placeholder 頁面

- [ ] **T2-1** 建立 `app/(site)/articles/page.tsx`（文章列表 placeholder）
- [ ] **T2-2** 建立 `app/(site)/guides/page.tsx`（攻略列表 placeholder）
- [ ] **T2-3** 建立 `app/countdown/page.tsx`（生命倒數 placeholder）
- [ ] **T2-4** 建立 `app/shop/page.tsx`（代購入口 placeholder）
- [ ] **T2-5** 建立 `components/layout/Navbar.tsx`（靜態導覽列，含四個連結）
- [ ] **T2-6** 建立 `components/layout/Footer.tsx`（靜態頁尾）
- [ ] **T2-7** 建立 `components/ui/` 空目錄（加 `.gitkeep`）
- [ ] **T2-8** 建立 `lib/api.ts`（fetch wrapper，讀 `NEXT_PUBLIC_API_URL`）
- [ ] **T2-9** 建立 `types/index.ts`（全域型別，空 export）
- [ ] **T2-10** 建立 `.env.local.example`，內容：
  ```env
  NEXT_PUBLIC_API_URL=http://localhost:8080
  ```
- [ ] **T2-11** 複製 `.env.local.example` 為 `.env.local`（本機使用）
- [ ] **T2-12** 確認 `.gitignore` 已包含 `.env.local`

---

## T3 — 前端設定檔調整

- [ ] **T3-1** 確認 `next.config.ts` 設定正確（無多餘 experimental flag）
- [ ] **T3-2** 確認 `app/layout.tsx` 有正確的 metadata（title、description）並引入 Navbar/Footer
- [ ] **T3-3** 確認 `app/globals.css` 引入 Tailwind directives
- [ ] **T3-4** 更新 `app/page.tsx` 首頁，顯示 Hero placeholder 文字與導覽用 CTA 連結

---

## T4 — 後端 Scaffold（Spring Initializr）

- [ ] **T4-1** 使用 curl 從 Spring Initializr 下載專案模板
  ```bash
  cd /Users/eyesc/Project/TravelProject
  curl https://start.spring.io/starter.zip \
    -d type=maven-project \
    -d language=java \
    -d bootVersion=3.4.1 \
    -d groupId=com.travel \
    -d artifactId=travel-backend \
    -d name=TravelBackend \
    -d packageName=com.travel \
    -d javaVersion=21 \
    -d dependencies=web,data-jpa,actuator,validation,h2,lombok,devtools \
    -o travel-backend.zip
  ```
- [ ] **T4-2** 解壓縮並移除壓縮檔
  ```bash
  unzip travel-backend.zip -d travel-backend && rm travel-backend.zip
  ```
- [ ] **T4-3** 確認 `pom.xml` 中 `<java.version>21</java.version>`
- [ ] **T4-4** 確認 `pom.xml` 包含所有必要 dependencies（web、jpa、actuator、validation、h2、lombok、devtools）

> **備用方案（若 curl 失敗）：** 手動建立 Maven 結構，pom.xml 依 plan 手動填寫

---

## T5 — 後端 Package 結構與 Placeholder 檔案

- [ ] **T5-1** 建立 `config/WebConfig.java`（CORS 設定，允許 `http://localhost:3000`）
  ```java
  @Configuration
  public class WebConfig implements WebMvcConfigurer {
      @Override
      public void addCorsMappings(CorsRegistry registry) {
          registry.addMapping("/api/**")
              .allowedOrigins("http://localhost:3000")
              .allowedMethods("GET","POST","PUT","DELETE","OPTIONS");
      }
  }
  ```
- [ ] **T5-2** 建立 `article/Article.java`（`@Entity` placeholder，含 `id`、`title` 欄位）
- [ ] **T5-3** 建立 `article/ArticleService.java`（`@Service` placeholder，空方法）
- [ ] **T5-4** 建立 `article/ArticleController.java`（`@RestController`，`GET /api/articles` 回傳空 List）
- [ ] **T5-5** 建立 `guide/Guide.java`（`@Entity` placeholder）
- [ ] **T5-6** 建立 `guide/GuideService.java`（`@Service` placeholder）
- [ ] **T5-7** 建立 `guide/GuideController.java`（`@RestController`，`GET /api/guides` 回傳空 List）
- [ ] **T5-8** 建立 `countdown/` 空目錄（加 `.gitkeep`）
- [ ] **T5-9** 建立 `shop/` 空目錄（加 `.gitkeep`）

---

## T6 — 後端設定檔

- [ ] **T6-1** 修改 `src/main/resources/application.yml`：
  ```yaml
  server:
    port: 8080

  spring:
    datasource:
      url: jdbc:h2:mem:traveldb
      driver-class-name: org.h2.Driver
      username: sa
      password:
    h2:
      console:
        enabled: true
        path: /h2-console
    jpa:
      database-platform: org.hibernate.dialect.H2Dialect
      hibernate:
        ddl-auto: create-drop
      show-sql: false

  management:
    endpoints:
      web:
        exposure:
          include: health,info
  ```
- [ ] **T6-2** 確認 `.gitignore` 不會 commit `application-local.yml`（若存在）

---

## T7 — 驗證

- [ ] **T7-1** 前端啟動測試
  ```bash
  cd travel-frontend && npm run dev
  # 確認 http://localhost:3000 顯示首頁
  ```
- [ ] **T7-2** 前端四個 route 可訪問
  - `http://localhost:3000/`
  - `http://localhost:3000/articles`
  - `http://localhost:3000/guides`
  - `http://localhost:3000/countdown`
- [ ] **T7-3** TypeScript 型別檢查通過
  ```bash
  cd travel-frontend && npx tsc --noEmit
  # 預期：0 errors
  ```
- [ ] **T7-4** 後端啟動測試
  ```bash
  cd travel-backend && ./mvnw spring-boot:run
  ```
- [ ] **T7-5** Actuator health check 通過
  ```bash
  curl http://localhost:8080/actuator/health
  # 預期：{"status":"UP"}
  ```
- [ ] **T7-6** CORS 確認（從前端 `localhost:3000` 呼叫後端不被擋）

---

## T8 — 收尾

- [ ] **T8-1** 建立 `travel-frontend/README.md`（啟動說明）
- [ ] **T8-2** 建立 `travel-backend/README.md`（啟動說明）
- [ ] **T8-3** 確認根目錄 `.gitignore` 包含：
  - `travel-frontend/.env.local`
  - `travel-frontend/node_modules/`
  - `travel-backend/target/`
- [ ] **T8-4** git add + commit（訊息：`feat: phase 1 scaffold — travel-frontend and travel-backend`）

---

## Done Checklist（對應 plan Done definition）

- [ ] `travel-frontend/` 可 `npm run dev` 啟動，首頁顯示正常
- [ ] 四個 route placeholder 可訪問：`/`、`/articles`、`/guides`、`/countdown`
- [ ] `npx tsc --noEmit` 0 errors
- [ ] `travel-backend/` 可 `./mvnw spring-boot:run` 啟動
- [ ] `GET /actuator/health` 回傳 `{"status":"UP"}`
- [ ] `WebConfig.java` CORS 允許 `http://localhost:3000`
- [ ] `.env.local` 已加入 `.gitignore`
